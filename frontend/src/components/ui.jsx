// export function PageHeader({ title, subtitle }) {
//   return (
//     <div style={{ marginBottom: "20px" }}>
//       <h1 style={{ fontSize: "26px", fontWeight: "700" }}>{title}</h1>
//       {subtitle && (
//         <p style={{ color: "#94a3b8", marginTop: "5px" }}>{subtitle}</p>
//       )}
//     </div>
//   );
// }

// export function GlassCard({ children }) {
//   return <div className="card">{children}</div>;
// }

// export function Grid({ children, cols = 3 }) {
//   return <div className={`grid grid-${cols}`}>{children}</div>;
// }